# Update the version tag below when you need to change the DataKit version
# Get the latest version from TrueWatch Platform (see README.md)
FROM pubrepo.truewatch.com/datakit/datakit:VERSION

EXPOSE 9529